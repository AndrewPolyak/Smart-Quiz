class Group:
    def __init__ (self, users, groupID):
        self.users = users #
        self.groupID = groupID #need database of all groupID's, and all userID's associated with it


#We need a file that stores all usernames and passwords
#We need a file that stores groupID's and the list of usernames that are in said group
#Join groups using group ID's



        